<Window x:Class="Schedule1ModdingTool.Views.QuestEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Height="700" Width="900"
        MinHeight="600" MinWidth="800"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{StaticResource DarkBackgroundBrush}"
        Foreground="{StaticResource LightTextBrush}">

    <Grid Background="{StaticResource DarkPanelBrush}" Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400" MinWidth="350"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" MinWidth="400"/>
            </Grid.ColumnDefinitions>

            <!-- Quest Properties Panel -->
            <Border Grid.Column="0"
                    Background="{StaticResource DarkPanelBrush}"
                    BorderBrush="{StaticResource DarkBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Margin="5">
                <DockPanel>
                    <TextBlock Text="Quest Properties" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource LightTextBrush}" Margin="12,12,12,8" DockPanel.Dock="Top"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="12">
                        <StackPanel>
                            <!-- Quest Details Section -->
                            <Border Background="{StaticResource DarkBackgroundBrush}" BorderBrush="{StaticResource DarkBorderBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <TextBlock Text="Quest Details" FontWeight="Bold" FontSize="14" Margin="0,0,0,12"/>
                                    
                                    <Label Content="Class Name:" FontSize="12"/>
                                    <TextBox Text="{Binding Quest.ClassName, UpdateSourceTrigger=PropertyChanged}" FontSize="12" Margin="0,0,0,6"/>
                                    
                                    <Label Content="Quest ID:" FontSize="12"/>
                                    <TextBox Text="{Binding Quest.QuestId, UpdateSourceTrigger=PropertyChanged}" FontSize="12" Margin="0,0,0,6"/>
                                    
                                    <Label Content="Quest Title:" FontSize="12"/>
                                    <TextBox Text="{Binding Quest.QuestTitle, UpdateSourceTrigger=PropertyChanged}" FontSize="12" Margin="0,0,0,6"/>
                                    
                                    <Label Content="Quest Description:" FontSize="12"/>
                                    <TextBox Text="{Binding Quest.QuestDescription, UpdateSourceTrigger=PropertyChanged}" 
                                             Height="80" 
                                             TextWrapping="Wrap" 
                                             AcceptsReturn="True" 
                                             VerticalScrollBarVisibility="Auto"
                                             FontSize="12"
                                             Margin="0,0,0,6"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- MelonLoader Mod Settings Section -->
                            <Border Background="{StaticResource DarkBackgroundBrush}" BorderBrush="{StaticResource DarkBorderBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <TextBlock Text="MelonLoader Mod Settings" FontWeight="Bold" FontSize="14" Margin="0,0,0,12"/>
                                    
                                    <Label Content="Mod Name:" FontSize="12"/>
                                    <TextBox Text="{Binding Quest.ModName, UpdateSourceTrigger=PropertyChanged}" FontSize="12" Margin="0,0,0,6"/>
                                    
                                    <Label Content="Mod Version:" FontSize="12"/>
                                    <TextBox Text="{Binding Quest.ModVersion, UpdateSourceTrigger=PropertyChanged}" FontSize="12" Margin="0,0,0,6"/>
                                    
                                    <Label Content="Mod Author:" FontSize="12"/>
                                    <TextBox Text="{Binding Quest.ModAuthor, UpdateSourceTrigger=PropertyChanged}" FontSize="12" Margin="0,0,0,6"/>
                                    
                                    <Label Content="Game Developer:" FontSize="12"/>
                                    <TextBox Text="{Binding Quest.GameDeveloper, UpdateSourceTrigger=PropertyChanged}" FontSize="12" Margin="0,0,0,6"/>
                                    
                                    <Label Content="Game Name:" FontSize="12"/>
                                    <TextBox Text="{Binding Quest.GameName, UpdateSourceTrigger=PropertyChanged}" FontSize="12" Margin="0,0,0,6"/>
                                    
                                    <Label Content="Namespace:" FontSize="12"/>
                                    <TextBox Text="{Binding Quest.Namespace, UpdateSourceTrigger=PropertyChanged}" FontSize="12" Margin="0,0,0,6"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- Quest Settings Section -->
                            <Border Background="{StaticResource DarkBackgroundBrush}" BorderBrush="{StaticResource DarkBorderBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <TextBlock Text="Quest Settings" FontWeight="Bold" FontSize="14" Margin="0,0,0,12"/>
                                    
                                    <CheckBox Content="Auto Begin" 
                                              IsChecked="{Binding Quest.AutoBegin}" 
                                              FontSize="12"
                                              Margin="0,3"/>
                                    
                                    <CheckBox Content="Custom Icon" 
                                              IsChecked="{Binding Quest.CustomIcon}" 
                                              FontSize="12"
                                              Margin="0,3"/>
                                    
                                    <CheckBox Content="Quest Rewards" 
                                              IsChecked="{Binding Quest.QuestRewards}" 
                                              FontSize="12"
                                              Margin="0,3"/>
                                    
                                    <CheckBox Content="Generate Data Class" 
                                              IsChecked="{Binding Quest.GenerateDataClass}" 
                                              FontSize="12"
                                              Margin="0,3"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- Quest Objectives Section -->
                            <Border Background="{StaticResource DarkBackgroundBrush}" BorderBrush="{StaticResource DarkBorderBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <DockPanel Margin="0,0,0,12">
                                        <TextBlock Text="Quest Objectives" FontWeight="Bold" FontSize="14" DockPanel.Dock="Left"/>
                                        <Button Content="+ Add Objective" 
                                                Command="{Binding AddObjectiveCommand}"
                                                DockPanel.Dock="Right"
                                                HorizontalAlignment="Right"
                                                FontSize="11"
                                                Padding="8,4"/>
                                    </DockPanel>
                                    
                                    <ItemsControl ItemsSource="{Binding Quest.Objectives}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{StaticResource DarkBackgroundBrush}" 
                                                        BorderBrush="{StaticResource DarkBorderBrush}" 
                                                        BorderThickness="1" 
                                                        CornerRadius="3" 
                                                        Margin="0,3" 
                                                        Padding="8">
                                                    <StackPanel>
                                                        <DockPanel>
                                                            <StackPanel DockPanel.Dock="Left">
                                                                <Label Content="Name:" FontSize="11"/>
                                                                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" FontSize="11"/>
                                                            </StackPanel>
                                                            
                                                            <Button DockPanel.Dock="Right" 
                                                                    Content="❌" 
                                                                    Command="{Binding DataContext.RemoveObjectiveCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding}"
                                                                    VerticalAlignment="Top"
                                                                    Width="24" Height="24"
                                                                    FontSize="10"/>
                                                        </DockPanel>
                                                        
                                                        <Label Content="Title:" FontSize="11"/>
                                                        <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" FontSize="11"/>
                                                        
                                                        <Label Content="Required Progress:" FontSize="11"/>
                                                        <TextBox Text="{Binding RequiredProgress, UpdateSourceTrigger=PropertyChanged}" FontSize="11"/>
                                                        
                                                        <CheckBox Content="Has Location" 
                                                                  IsChecked="{Binding HasLocation}" 
                                                                  FontSize="11" Margin="0,6,0,3"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <!-- Vertical Splitter -->
            <GridSplitter Grid.Column="1"
                          Width="5"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Stretch"
                          Style="{StaticResource SplitterStyle}"/>

            <!-- Code Preview Panel -->
            <Border Grid.Column="2"
                    Background="{StaticResource DarkPanelBrush}"
                    BorderBrush="{StaticResource DarkBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Margin="5">
                <DockPanel>
                    <DockPanel DockPanel.Dock="Top" Margin="12,12,12,8">
                        <TextBlock Text="Generated C# Code" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource LightTextBrush}" DockPanel.Dock="Left"/>
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                            <Button Content="Regenerate" 
                                    Command="{Binding RegenerateCodeCommand}" 
                                    Margin="4,0,0,0"
                                    FontSize="11"
                                    Padding="8,4"/>
                            <Button Content="Copy Code" 
                                    Command="{Binding CopyCodeCommand}" 
                                    Margin="4,0,0,0"
                                    FontSize="11"
                                    Padding="8,4"/>
                        </StackPanel>
                    </DockPanel>
                    <TextBox
                        x:Name="CodeEditor"
                        FontFamily="Consolas"
                        FontSize="11"
                        Text="{Binding GeneratedCode, Mode=OneWay}"
                        IsReadOnly="True"
                        Margin="12,0,12,12"
                        VerticalScrollBarVisibility="Auto"
                        HorizontalScrollBarVisibility="Auto"
                        AcceptsReturn="True"
                        TextWrapping="NoWrap"
                        Background="{StaticResource DarkBackgroundBrush}"
                        Foreground="{StaticResource LightTextBrush}"/>
                </DockPanel>
            </Border>
        </Grid>

        <!-- Bottom Button Panel -->
        <Border Grid.Row="1"
                Background="{StaticResource DarkPanelBrush}"
                BorderBrush="{StaticResource DarkBorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Apply Changes" 
                        Command="{Binding ApplyChangesCommand}" 
                        IsDefault="True"
                        Margin="0,0,8,0"
                        Padding="16,6"
                        FontSize="12"/>
                <Button Content="Cancel" 
                        Command="{Binding CancelCommand}" 
                        IsCancel="True"
                        Padding="16,6"
                        FontSize="12"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
