<UserControl x:Class="Schedule1ModdingTool.Views.NpcPropertiesControl"
             x:Name="RootControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:Schedule1ModdingTool.Models"
             xmlns:presets="clr-namespace:Schedule1ModdingTool.Data"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:views="clr-namespace:Schedule1ModdingTool.Views"
             xmlns:controls="clr-namespace:Schedule1ModdingTool.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <DataTemplate x:Key="NpcGlobalStateSetterTemplate" DataType="{x:Type models:GlobalStateSetterBlueprint}">
            <Border BorderBrush="{StaticResource DarkBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="8" Margin="0,0,0,6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Row="0"
                              Grid.Column="0"
                              ItemsSource="{Binding DataContext.AvailableGlobalStateFieldReferences, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              DisplayMemberPath="DisplayLabel"
                              Style="{StaticResource ComboBoxStyle}"
                              Margin="0,0,8,8"
                              Tag="{Binding}"
                              Loaded="NpcGlobalStateFieldComboBox_Loaded"
                              SelectionChanged="NpcGlobalStateFieldComboBox_SelectionChanged"/>
                    <TextBox Grid.Row="0"
                             Grid.Column="1"
                             Text="{Binding NewValue, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource TextBoxStyle}"
                             Margin="0,0,8,8"/>
                    <CheckBox Grid.Row="0"
                              Grid.Column="2"
                              Content="Save"
                              IsChecked="{Binding RequestSave, UpdateSourceTrigger=PropertyChanged}"
                              Foreground="{StaticResource LightTextBrush}"
                              VerticalAlignment="Center"
                              Margin="0,0,8,8"/>
                    <Button Grid.Row="0"
                            Grid.Column="3"
                            Content="Remove"
                            Tag="{Binding}"
                            Click="RemoveGeneratedActionGlobalStateSetter_Click"
                            Style="{StaticResource IconButtonStyle}"
                            Margin="0,0,0,8"/>
                    <TextBlock Grid.Row="1"
                               Grid.Column="0"
                               Grid.ColumnSpan="4"
                               Text="{Binding DisplayName, StringFormat='Target: {0}'}"
                               Foreground="{StaticResource MediumTextBrush}"
                               FontSize="10"
                               TextWrapping="Wrap"/>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="8" Loaded="Grid_Loaded" VerticalAlignment="Stretch">
        <TabControl x:Name="NpcEditorTabControl" Style="{StaticResource MaterialDesignNavigationRailTabControl}"
                    materialDesign:ColorZoneAssist.Mode="PrimaryMid"
                    VerticalAlignment="Stretch"
                    SelectionChanged="NpcEditorTabControl_SelectionChanged">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem" BasedOn="{StaticResource NavigationRailTabItemStyle}"/>
            </TabControl.ItemContainerStyle>

            <!-- Tab 1: Identity & Setup -->
            <TabItem Header="Identity &amp; Setup">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="AccountCircle" 
                                                   VerticalAlignment="Center" 
                                                   Margin="0,0,8,0"
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=TabItem}, Path=Foreground}"/>
                            <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>

                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid Margin="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left Column -->
                        <StackPanel Grid.Column="0">
                            <!-- Identity Panel -->
                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <TextBlock Text="Identity" Style="{StaticResource PanelHeaderStyle}" Margin="0,0,0,12"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Class Name" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon PropertyName="ClassName"/>
                                    </StackPanel>
                                    <controls:ValidatedTextBox Text="{Binding SelectedNpc.ClassName, UpdateSourceTrigger=PropertyChanged}"
                                                               ValidationType="ClassName"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Namespace" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon PropertyName="Namespace"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.Namespace, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="NPC ID" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon PropertyName="NpcId"/>
                                    </StackPanel>
                                    <controls:ValidatedTextBox Text="{Binding SelectedNpc.NpcId, UpdateSourceTrigger=PropertyChanged}"
                                                               ValidationType="NpcId"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="First Name" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon PropertyName="FirstName"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.FirstName, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Last Name" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon PropertyName="LastName"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.LastName, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>
                                </StackPanel>
                            </Border>

                            <!-- Behavior Panel -->
                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <TextBlock Text="Behavior" Style="{StaticResource PanelHeaderStyle}" Margin="0,0,0,12"/>

                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="Is Physical" IsChecked="{Binding SelectedNpc.IsPhysical}"
                                                  Foreground="{StaticResource LightTextBrush}" Margin="0,2"/>
                                        <views:InfoIcon PropertyName="IsPhysical"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="Enable Customer Systems"
                                                  IsChecked="{Binding SelectedNpc.EnableCustomer}"
                                                  Checked="EnableCustomer_Checked"
                                                  Unchecked="EnableCustomer_Unchecked"
                                                  Foreground="{StaticResource LightTextBrush}" Margin="0,2"/>
                                        <views:InfoIcon PropertyName="EnableCustomer"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="Is Dealer"
                                                  IsChecked="{Binding SelectedNpc.IsDealer}"
                                                  Checked="IsDealer_Checked"
                                                  Unchecked="IsDealer_Unchecked"
                                                  Foreground="{StaticResource LightTextBrush}" Margin="0,2"/>
                                        <views:InfoIcon PropertyName="IsDealer"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <!-- Right Column -->
                        <StackPanel Grid.Column="2">
                            <!-- Spawn Position Panel -->
                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <TextBlock Text="Spawn Position" Style="{StaticResource PanelHeaderStyle}" Margin="0,0,0,12"/>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="Set spawn position"
                                                  IsChecked="{Binding SelectedNpc.HasSpawnPosition}"
                                                  Foreground="{StaticResource LightTextBrush}" Margin="0,0,0,8"/>
                                        <views:InfoIcon PropertyName="HasSpawnPosition"/>
                                    </StackPanel>
                                    <controls:Vector3Input X="{Binding SelectedNpc.SpawnX, Mode=TwoWay}"
                                                         Y="{Binding SelectedNpc.SpawnY, Mode=TwoWay}"
                                                         Z="{Binding SelectedNpc.SpawnZ, Mode=TwoWay}"/>
                                    <Button Content="Use Current Player Position"
                                            Style="{StaticResource IconButtonStyle}"
                                            Click="SetSpawnPositionFromPlayer_Click"
                                            Margin="0,8,0,0"
                                            ToolTip="Sets the spawn position to the current player position in the Main scene (requires connector mod running)"/>
                                </StackPanel>
                            </Border>

                            <!-- Relationship Defaults Panel -->
                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <TextBlock Text="Relationship Defaults" Style="{StaticResource PanelHeaderStyle}" Margin="0,0,0,12"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Starting Relationship Delta" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Initial relationship level (0-5): 0=Stranger, 1=Acquaintance, 2.5=Friend, 4=Good Friend, 5=Best Friend"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.RelationshipDefaults.StartingDelta, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <CheckBox Content="Starts Unlocked"
                                                  IsChecked="{Binding SelectedNpc.RelationshipDefaults.StartsUnlocked}"
                                                  Foreground="{StaticResource LightTextBrush}"/>
                                        <views:InfoIcon TooltipText="If checked, NPC is immediately available for interaction"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <Label Content="Unlock Type" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="How the NPC becomes available: DirectApproach = player can approach directly, Recommendation = requires introduction from connected NPC"/>
                                    </StackPanel>
                                    <ComboBox SelectedValue="{Binding SelectedNpc.RelationshipDefaults.UnlockType}"
                                              SelectedValuePath="Content"
                                              Style="{StaticResource ComboBoxStyle}">
                                        <ComboBoxItem Content="DirectApproach"/>
                                        <ComboBoxItem Content="Recommendation"/>
                                    </ComboBox>

                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <Label Content="Connected NPCs" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="NPCs in this NPC's social network. Used for recommendations and relationship propagation"/>
                                    </StackPanel>
                                    <ListBox x:Name="ConnectionsListBox"
                                             ItemsSource="{Binding SelectedNpc.RelationshipDefaults.Connections}"
                                             Height="100"
                                             Background="{StaticResource DarkBackgroundBrush}"
                                             Foreground="{StaticResource LightTextBrush}"
                                             BorderBrush="{StaticResource DarkBorderBrush}"
                                             Margin="0,0,0,4"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <views:NpcSelector x:Name="ConnectionNpcSelector"
                                                           Grid.Column="0"
                                                           Margin="0,0,4,0"/>
                                        <Button Grid.Column="1" Content="Add"
                                                Style="{StaticResource IconButtonStyle}"
                                                Click="AddConnection_Click"
                                                Margin="0,0,4,0"/>
                                        <Button Grid.Column="2" Content="Remove"
                                                Style="{StaticResource IconButtonStyle}"
                                                Click="RemoveConnection_Click"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 2: Appearance -->
            <TabItem Header="Appearance">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Palette" 
                                                   VerticalAlignment="Center" 
                                                   Margin="0,0,8,0"
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=TabItem}, Path=Foreground}"/>
                            <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>

                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid Margin="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="3*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left Column: Physical Attributes & Hair -->
                        <StackPanel Grid.Column="0">
                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <TextBlock Text="Physical Attributes" Style="{StaticResource PanelHeaderStyle}" Margin="0,0,0,12"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Gender (0-1)" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Gender value: 0.0 = male, 1.0 = female. Values between 0 and 1 blend characteristics"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.Appearance.Gender, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Height" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="NPC height value (0.0 = short, 1.0 = tall). Affects overall scale and proportions"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.Appearance.Height, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Weight" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="NPC weight value (0.0 = thin, 1.0 = heavy). Affects body proportions and shape"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.Appearance.Weight, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Pupil Dilation" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Controls pupil dilation (0.0-1.0). Higher values = more dilated pupils"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.Appearance.PupilDilation, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Eyebrow Scale" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Scale factor for eyebrow size (0.0-1.0). Affects eyebrow width"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.Appearance.EyebrowScale, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Eyebrow Thickness" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Thickness value for eyebrows (0.0-1.0). Higher values = thicker eyebrows"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.Appearance.EyebrowThickness, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource PanelStyle}">
                                <StackPanel Margin="12">
                                    <TextBlock Text="Hair" Style="{StaticResource PanelHeaderStyle}" Margin="0,0,0,12"/>
                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Hair Path" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Resource path to the hair style asset (e.g., 'Avatar/Hair/Spiky/Spiky'). Use presets or enter custom path"/>
                                    </StackPanel>
                                    <ComboBox ItemsSource="{x:Static presets:AppearancePresets.HairStyles}"
                                              IsEditable="True"
                                              Text="{Binding SelectedNpc.Appearance.HairPath, UpdateSourceTrigger=PropertyChanged}">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Name}"/>
                                                    <TextBlock Text="{Binding Path}" FontSize="11" Foreground="{StaticResource DimTextBrush}"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <!-- Right Column: Colors & Layers -->
                        <StackPanel Grid.Column="2">
                            <!-- Base Colors -->
                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <TextBlock Text="Base Colors" Style="{StaticResource PanelHeaderStyle}" Margin="0,0,0,12"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,4">
                                        <Label Content="Skin" Width="130"/>
                                        <TextBox Text="{Binding SelectedNpc.Appearance.SkinColor, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                                        <Border Width="32" Height="24" Margin="8,0"
                                                Background="{Binding SelectedNpc.Appearance.SkinColor, Converter={StaticResource ColorHexToBrushConverter}}"/>
                                        <Button Content="Pick" Tag="SkinColor" Style="{StaticResource IconButtonStyle}"
                                                Click="PickAppearanceColor_Click"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,4">
                                        <Label Content="Left Eyelid" Width="130"/>
                                        <TextBox Text="{Binding SelectedNpc.Appearance.LeftEyeLidColor, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                                        <Border Width="32" Height="24" Margin="8,0"
                                                Background="{Binding SelectedNpc.Appearance.LeftEyeLidColor, Converter={StaticResource ColorHexToBrushConverter}}"/>
                                        <Button Content="Pick" Tag="LeftEyeLidColor" Style="{StaticResource IconButtonStyle}"
                                                Click="PickAppearanceColor_Click"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,4">
                                        <Label Content="Right Eyelid" Width="130"/>
                                        <TextBox Text="{Binding SelectedNpc.Appearance.RightEyeLidColor, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                                        <Border Width="32" Height="24" Margin="8,0"
                                                Background="{Binding SelectedNpc.Appearance.RightEyeLidColor, Converter={StaticResource ColorHexToBrushConverter}}"/>
                                        <Button Content="Pick" Tag="RightEyeLidColor" Style="{StaticResource IconButtonStyle}"
                                                Click="PickAppearanceColor_Click"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,4">
                                        <Label Content="Eyeball Tint" Width="130"/>
                                        <TextBox Text="{Binding SelectedNpc.Appearance.EyeBallTint, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                                        <Border Width="32" Height="24" Margin="8,0"
                                                Background="{Binding SelectedNpc.Appearance.EyeBallTint, Converter={StaticResource ColorHexToBrushConverter}}"/>
                                        <Button Content="Pick" Tag="EyeBallTint" Style="{StaticResource IconButtonStyle}"
                                                Click="PickAppearanceColor_Click"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,4">
                                        <Label Content="Hair" Width="130"/>
                                        <TextBox Text="{Binding SelectedNpc.Appearance.HairColor, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                                        <Border Width="32" Height="24" Margin="8,0"
                                                Background="{Binding SelectedNpc.Appearance.HairColor, Converter={StaticResource ColorHexToBrushConverter}}"/>
                                        <Button Content="Pick" Tag="HairColor" Style="{StaticResource IconButtonStyle}"
                                                Click="PickAppearanceColor_Click"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- Face Layers -->
                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                        <TextBlock Text="Face Layers" Style="{StaticResource PanelHeaderStyle}"/>
                                        <views:InfoIcon TooltipText="Face layers define facial features. At least one face layer is required."/>
                                    </StackPanel>
                                    <ItemsControl ItemsSource="{Binding SelectedNpc.Appearance.FaceLayers}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type models:NpcAppearanceLayer}">
                                                <Grid Margin="0,4">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="120"/>
                                                        <ColumnDefinition Width="32"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ComboBox Grid.Column="0"
                                                              ItemsSource="{x:Static presets:AppearancePresets.FaceLayers}"
                                                              IsEditable="True"
                                                              Text="{Binding LayerPath, UpdateSourceTrigger=PropertyChanged}"
                                                              Margin="0,0,8,0">
                                                        <ComboBox.ItemTemplate>
                                                            <DataTemplate>
                                                                <StackPanel>
                                                                    <TextBlock Text="{Binding Name}"/>
                                                                    <TextBlock Text="{Binding Path}" FontSize="11" Foreground="{StaticResource DimTextBrush}"/>
                                                                </StackPanel>
                                                            </DataTemplate>
                                                        </ComboBox.ItemTemplate>
                                                    </ComboBox>
                                                    <TextBox Grid.Column="1" Text="{Binding ColorHex, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
                                                    <Border Grid.Column="2" Width="24" Height="24" Margin="0,0,8,0"
                                                            Background="{Binding ColorHex, Converter={StaticResource ColorHexToBrushConverter}}"/>
                                                    <StackPanel Grid.Column="3" Orientation="Horizontal">
                                                        <Button Content="Pick" Tag="{Binding}" Style="{StaticResource IconButtonStyle}"
                                                                Click="PickLayerColor_Click" Margin="0,0,8,0"/>
                                                        <Button Content="✕" Style="{StaticResource IconButtonStyle}"
                                                                Click="RemoveFaceLayer_Click" Tag="{Binding}"/>
                                                    </StackPanel>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <Button Content="Add Face Layer" Style="{StaticResource IconButtonStyle}"
                                            Click="AddFaceLayer_Click" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Body Layers -->
                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                        <TextBlock Text="Body Layers" Style="{StaticResource PanelHeaderStyle}"/>
                                        <views:InfoIcon TooltipText="Body layers define clothing and body textures."/>
                                    </StackPanel>
                                    <ItemsControl ItemsSource="{Binding SelectedNpc.Appearance.BodyLayers}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type models:NpcAppearanceLayer}">
                                                <Grid Margin="0,4">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="120"/>
                                                        <ColumnDefinition Width="32"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ComboBox Grid.Column="0"
                                                              ItemsSource="{x:Static presets:AppearancePresets.BodyLayers}"
                                                              IsEditable="True"
                                                              Text="{Binding LayerPath, UpdateSourceTrigger=PropertyChanged}"
                                                              Margin="0,0,8,0">
                                                        <ComboBox.ItemTemplate>
                                                            <DataTemplate>
                                                                <StackPanel>
                                                                    <TextBlock Text="{Binding Name}"/>
                                                                    <TextBlock Text="{Binding Path}" FontSize="11" Foreground="{StaticResource DimTextBrush}"/>
                                                                </StackPanel>
                                                            </DataTemplate>
                                                        </ComboBox.ItemTemplate>
                                                    </ComboBox>
                                                    <TextBox Grid.Column="1" Text="{Binding ColorHex, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
                                                    <Border Grid.Column="2" Width="24" Height="24" Margin="0,0,8,0"
                                                            Background="{Binding ColorHex, Converter={StaticResource ColorHexToBrushConverter}}"/>
                                                    <StackPanel Grid.Column="3" Orientation="Horizontal">
                                                        <Button Content="Pick" Tag="{Binding}" Style="{StaticResource IconButtonStyle}"
                                                                Click="PickLayerColor_Click" Margin="0,0,8,0"/>
                                                        <Button Content="✕" Style="{StaticResource IconButtonStyle}"
                                                                Click="RemoveBodyLayer_Click" Tag="{Binding}"/>
                                                    </StackPanel>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <Button Content="Add Body Layer" Style="{StaticResource IconButtonStyle}"
                                            Click="AddBodyLayer_Click" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Accessory Layers -->
                            <Border Style="{StaticResource PanelStyle}">
                                <StackPanel Margin="12">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                        <TextBlock Text="Accessory Layers" Style="{StaticResource PanelHeaderStyle}"/>
                                        <views:InfoIcon TooltipText="Accessory layers define items like shoes, hats, etc."/>
                                    </StackPanel>
                                    <ItemsControl ItemsSource="{Binding SelectedNpc.Appearance.AccessoryLayers}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type models:NpcAppearanceLayer}">
                                                <Grid Margin="0,4">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="120"/>
                                                        <ColumnDefinition Width="32"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ComboBox Grid.Column="0"
                                                              ItemsSource="{x:Static presets:AppearancePresets.AccessoryLayers}"
                                                              IsEditable="True"
                                                              Text="{Binding LayerPath, UpdateSourceTrigger=PropertyChanged}"
                                                              Margin="0,0,8,0">
                                                        <ComboBox.ItemTemplate>
                                                            <DataTemplate>
                                                                <StackPanel>
                                                                    <TextBlock Text="{Binding Name}"/>
                                                                    <TextBlock Text="{Binding Path}" FontSize="11" Foreground="{StaticResource DimTextBrush}"/>
                                                                </StackPanel>
                                                            </DataTemplate>
                                                        </ComboBox.ItemTemplate>
                                                    </ComboBox>
                                                    <TextBox Grid.Column="1" Text="{Binding ColorHex, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
                                                    <Border Grid.Column="2" Width="24" Height="24" Margin="0,0,8,0"
                                                            Background="{Binding ColorHex, Converter={StaticResource ColorHexToBrushConverter}}"/>
                                                    <StackPanel Grid.Column="3" Orientation="Horizontal">
                                                        <Button Content="Pick" Tag="{Binding}" Style="{StaticResource IconButtonStyle}"
                                                                Click="PickLayerColor_Click" Margin="0,0,8,0"/>
                                                        <Button Content="✕" Style="{StaticResource IconButtonStyle}"
                                                                Click="RemoveAccessoryLayer_Click" Tag="{Binding}"/>
                                                    </StackPanel>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <Button Content="Add Accessory Layer" Style="{StaticResource IconButtonStyle}"
                                            Click="AddAccessoryLayer_Click" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 3: Schedule -->
            <TabItem Header="Schedule">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ClockOutline" 
                                                   VerticalAlignment="Center" 
                                                   Margin="0,0,8,0"
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=TabItem}, Path=Foreground}"/>
                            <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>

                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Schedule Actions List -->
                    <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource PanelStyle}">
                        <StackPanel Margin="12">
                            <DockPanel Margin="0,0,0,12">
                                <TextBlock Text="Schedule Actions" Style="{StaticResource PanelHeaderStyle}" DockPanel.Dock="Left"/>
                                <Button Content="+ Add Action"
                                        Style="{StaticResource IconButtonStyle}"
                                        Click="AddScheduleAction_Click"
                                        DockPanel.Dock="Right"/>
                            </DockPanel>

                            <ListBox x:Name="ScheduleActionsListBox"
                                     ItemsSource="{Binding SelectedNpc.ScheduleActions}"
                                     SelectedItem="{Binding SelectedScheduleAction, Mode=TwoWay}"
                                     Background="{StaticResource DarkBackgroundBrush}"
                                     Foreground="{StaticResource LightTextBrush}"
                                     BorderBrush="{StaticResource DarkBorderBrush}"
                                     Height="400">
                                <ListBox.ItemTemplate>
                                    <DataTemplate DataType="{x:Type models:NpcScheduleAction}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding StartTime, StringFormat='[{0:D4}]'}" FontFamily="Consolas" Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding ActionType}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <Button Content="Remove Selected"
                                    Style="{StaticResource IconButtonStyle}"
                                    Click="RemoveScheduleAction_Click"
                                    Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Action Editor -->
                    <ScrollViewer Grid.Row="0" Grid.Column="2" VerticalScrollBarVisibility="Auto">
                        <controls:ScheduleActionEditor DataContext="{Binding SelectedScheduleAction}"/>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- Tab 4: Customer Settings -->
            <TabItem Header="Customer" Visibility="{Binding SelectedNpc.EnableCustomer, Converter={StaticResource BoolToVisibilityConverter}}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Cart" 
                                                   VerticalAlignment="Center" 
                                                   Margin="0,0,8,0"
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=TabItem}, Path=Foreground}"/>
                            <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>

                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid Margin="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left Column: Basic Settings -->
                        <StackPanel Grid.Column="0">
                            <!-- Spending & Orders -->
                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <TextBlock Text="Spending &amp; Orders" Style="{StaticResource PanelHeaderStyle}" Margin="0,0,0,12"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Min Weekly Spending" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Minimum amount ($) this customer will spend per week on products"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.CustomerDefaults.MinWeeklySpending, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Max Weekly Spending" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Maximum amount ($) this customer will spend per week on products"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.CustomerDefaults.MaxWeeklySpending, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Min Orders Per Week" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Minimum number of orders this customer will place per week"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.CustomerDefaults.MinOrdersPerWeek, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Max Orders Per Week" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Maximum number of orders this customer will place per week"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.CustomerDefaults.MaxOrdersPerWeek, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Preferred Order Day" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Day of the week when this customer prefers to place orders"/>
                                    </StackPanel>
                                    <ComboBox SelectedValue="{Binding SelectedNpc.CustomerDefaults.PreferredOrderDay, Mode=TwoWay}"
                                              SelectedValuePath="Content"
                                              Style="{StaticResource ComboBoxStyle}">
                                        <ComboBoxItem Content="Monday"/>
                                        <ComboBoxItem Content="Tuesday"/>
                                        <ComboBoxItem Content="Wednesday"/>
                                        <ComboBoxItem Content="Thursday"/>
                                        <ComboBoxItem Content="Friday"/>
                                        <ComboBoxItem Content="Saturday"/>
                                        <ComboBoxItem Content="Sunday"/>
                                    </ComboBox>

                                    <controls:TimeInput Time="{Binding SelectedNpc.CustomerDefaults.OrderTime, Mode=TwoWay}"
                                                      Label="Order Time"
                                                      Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Customer Behavior -->
                            <Border Style="{StaticResource PanelStyle}">
                                <StackPanel Margin="12">
                                    <TextBlock Text="Customer Behavior" Style="{StaticResource PanelHeaderStyle}" Margin="0,0,0,12"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Customer Standards" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Quality expectations: VeryLow/Low = accepts low quality, Moderate = expects decent quality, High/VeryHigh = demands high quality"/>
                                    </StackPanel>
                                    <ComboBox SelectedValue="{Binding SelectedNpc.CustomerDefaults.CustomerStandards, Mode=TwoWay}"
                                              SelectedValuePath="Content"
                                              Style="{StaticResource ComboBoxStyle}">
                                        <ComboBoxItem Content="VeryLow"/>
                                        <ComboBoxItem Content="Low"/>
                                        <ComboBoxItem Content="Moderate"/>
                                        <ComboBoxItem Content="High"/>
                                        <ComboBoxItem Content="VeryHigh"/>
                                    </ComboBox>

                                    <StackPanel Orientation="Horizontal" Margin="0,8">
                                        <CheckBox Content="Allow Direct Approach"
                                                  IsChecked="{Binding SelectedNpc.CustomerDefaults.AllowDirectApproach}"
                                                  Foreground="{StaticResource LightTextBrush}"/>
                                        <views:InfoIcon TooltipText="If enabled, player can approach this customer directly without introduction"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="Guarantee First Sample"
                                                  IsChecked="{Binding SelectedNpc.CustomerDefaults.GuaranteeFirstSample}"
                                                  Foreground="{StaticResource LightTextBrush}"/>
                                        <views:InfoIcon TooltipText="If enabled, customer will always accept the first sample product offered"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <!-- Right Column: Advanced Settings -->
                        <StackPanel Grid.Column="2">
                            <!-- Relationship Requirements -->
                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                        <TextBlock Text="Relationship Requirements" Style="{StaticResource PanelHeaderStyle}"/>
                                        <views:InfoIcon TooltipText="Relationship level required to buy products at different quality levels"/>
                                    </StackPanel>

                                    <Label Content="Min (at 50% quality)" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding SelectedNpc.CustomerDefaults.MutualRelationMinAt50, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>

                                    <Label Content="Max (at 100% quality)" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding SelectedNpc.CustomerDefaults.MutualRelationMaxAt100, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>
                                </StackPanel>
                            </Border>

                            <!-- Risk & Addiction -->
                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <TextBlock Text="Risk &amp; Addiction" Style="{StaticResource PanelHeaderStyle}" Margin="0,0,0,12"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Call Police Chance (0-1)" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Probability (0.0-1.0) that customer will call police during a deal. 0 = never, 1 = always"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.CustomerDefaults.CallPoliceChance, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Base Addiction" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Base addiction level (0.0-1.0). Higher values = more addicted, affects buying behavior"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.CustomerDefaults.BaseAddiction, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Dependence Multiplier" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Multiplier for addiction growth over time. Higher values = faster addiction increase"/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.CustomerDefaults.DependenceMultiplier, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource TextBoxStyle}"/>
                                </StackPanel>
                            </Border>

                            <!-- Drug Affinities -->
                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <controls:DrugAffinityEditor DrugAffinities="{Binding SelectedNpc.CustomerDefaults.DrugAffinities}"/>
                                </StackPanel>
                            </Border>

                            <!-- Preferred Properties -->
                            <Border Style="{StaticResource PanelStyle}">
                                <StackPanel Margin="12">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                        <TextBlock Text="Preferred Properties" Style="{StaticResource PanelHeaderStyle}"/>
                                        <views:InfoIcon TooltipText="Product properties this customer prefers"/>
                                    </StackPanel>

                                    <ListBox x:Name="PreferredPropertiesListBox"
                                             ItemsSource="{Binding SelectedNpc.CustomerDefaults.PreferredProperties}"
                                             Height="120"
                                             Background="{StaticResource DarkBackgroundBrush}"
                                             Foreground="{StaticResource LightTextBrush}"
                                             BorderBrush="{StaticResource DarkBorderBrush}"
                                             Margin="0,0,0,4"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <ComboBox x:Name="PreferredPropertyComboBox"
                                                  Grid.Column="0"
                                                  Style="{StaticResource ComboBoxStyle}"
                                                  Margin="0,0,4,0">
                                            <ComboBoxItem Content="AntiGravity"/>
                                            <ComboBoxItem Content="Athletic"/>
                                            <ComboBoxItem Content="Balding"/>
                                            <ComboBoxItem Content="BrightEyed"/>
                                            <ComboBoxItem Content="Calming"/>
                                            <ComboBoxItem Content="CalorieDense"/>
                                            <ComboBoxItem Content="Cyclopean"/>
                                            <ComboBoxItem Content="Disorienting"/>
                                            <ComboBoxItem Content="Electrifying"/>
                                            <ComboBoxItem Content="Energizing"/>
                                            <ComboBoxItem Content="Euphoric"/>
                                            <ComboBoxItem Content="Explosive"/>
                                            <ComboBoxItem Content="Focused"/>
                                            <ComboBoxItem Content="Foggy"/>
                                            <ComboBoxItem Content="Gingeritis"/>
                                            <ComboBoxItem Content="Glowie"/>
                                            <ComboBoxItem Content="Hallucinogenic"/>
                                            <ComboBoxItem Content="Jennerising"/>
                                            <ComboBoxItem Content="Laxative"/>
                                            <ComboBoxItem Content="Lethal"/>
                                            <ComboBoxItem Content="LongFaced"/>
                                            <ComboBoxItem Content="Munchies"/>
                                            <ComboBoxItem Content="Paranoia"/>
                                            <ComboBoxItem Content="Refreshing"/>
                                            <ComboBoxItem Content="Relaxing"/>
                                            <ComboBoxItem Content="Schizophrenic"/>
                                            <ComboBoxItem Content="Sedating"/>
                                            <ComboBoxItem Content="Seizure"/>
                                            <ComboBoxItem Content="Shrinking"/>
                                            <ComboBoxItem Content="Slippery"/>
                                            <ComboBoxItem Content="Smelly"/>
                                            <ComboBoxItem Content="Sneaky"/>
                                            <ComboBoxItem Content="Spicy"/>
                                            <ComboBoxItem Content="ThoughtProvoking"/>
                                            <ComboBoxItem Content="Toxic"/>
                                            <ComboBoxItem Content="TropicThunder"/>
                                            <ComboBoxItem Content="Zombifying"/>
                                        </ComboBox>
                                        <Button Grid.Column="1" Content="Add"
                                                Style="{StaticResource IconButtonStyle}"
                                                Click="AddPreferredProperty_Click"
                                                Margin="0,0,4,0"/>
                                        <Button Grid.Column="2" Content="Remove"
                                                Style="{StaticResource IconButtonStyle}"
                                                Click="RemovePreferredProperty_Click"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 5: Dealer Settings -->
            <TabItem Header="Dealer" Visibility="{Binding SelectedNpc.IsDealer, Converter={StaticResource BoolToVisibilityConverter}}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="AccountTie" 
                                                   VerticalAlignment="Center" 
                                                   Margin="0,0,8,0"
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=TabItem}, Path=Foreground}"/>
                            <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>

                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="12" MaxWidth="600" HorizontalAlignment="Left">
                        <!-- Economic Settings -->
                        <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                            <StackPanel Margin="12">
                                    <TextBlock Text="Economic Settings" Style="{StaticResource PanelHeaderStyle}" Margin="0,0,0,12"/>

                                <StackPanel Orientation="Horizontal">
                                    <Label Content="Signing Fee ($)" Style="{StaticResource LabelStyle}"/>
                                    <views:InfoIcon TooltipText="One-time fee ($) player must pay to recruit this dealer"/>
                                </StackPanel>
                                <TextBox Text="{Binding SelectedNpc.DealerDefaults.SigningFee, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource TextBoxStyle}"/>

                                <StackPanel Orientation="Horizontal">
                                    <Label Content="Commission Cut (0-1)" Style="{StaticResource LabelStyle}"/>
                                    <views:InfoIcon TooltipText="Dealer's commission rate (0.0-1.0). 0.15 = 15% commission on sales"/>
                                </StackPanel>
                                <TextBox Text="{Binding SelectedNpc.DealerDefaults.CommissionCut, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource TextBoxStyle}"/>

                                <StackPanel Orientation="Horizontal">
                                    <Label Content="Dealer Type" Style="{StaticResource LabelStyle}"/>
                                    <views:InfoIcon TooltipText="PlayerDealer = works for player, IndependentDealer = operates independently, RivalDealer = competitor"/>
                                </StackPanel>
                                <ComboBox SelectedItem="{Binding SelectedNpc.DealerDefaults.DealerType}"
                                          Style="{StaticResource ComboBoxStyle}">
                                    <ComboBoxItem Content="PlayerDealer"/>
                                    <ComboBoxItem Content="IndependentDealer"/>
                                    <ComboBoxItem Content="RivalDealer"/>
                                </ComboBox>
                            </StackPanel>
                        </Border>

                        <!-- Location & Behavior -->
                        <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                            <StackPanel Margin="12">
                                <TextBlock Text="Location &amp; Behavior" Style="{StaticResource PanelHeaderStyle}" Margin="0,0,0,12"/>

                                <StackPanel Orientation="Horizontal">
                                    <Label Content="Home Building Name" Style="{StaticResource LabelStyle}"/>
                                    <views:InfoIcon TooltipText="The building where this dealer lives/operates from"/>
                                </StackPanel>
                                <TextBox Text="{Binding SelectedNpc.DealerDefaults.HomeName, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource TextBoxStyle}"/>

                                <StackPanel Orientation="Horizontal" Margin="0,8">
                                    <CheckBox Content="Allow Insufficient Quality"
                                              IsChecked="{Binding SelectedNpc.DealerDefaults.AllowInsufficientQuality}"
                                              Foreground="{StaticResource LightTextBrush}"/>
                                    <views:InfoIcon TooltipText="Allow selling products below customer's quality expectations"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="Allow Excess Quality"
                                              IsChecked="{Binding SelectedNpc.DealerDefaults.AllowExcessQuality}"
                                              Foreground="{StaticResource LightTextBrush}"/>
                                    <views:InfoIcon TooltipText="Allow selling products above customer's quality expectations"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Tracking -->
                        <Border Style="{StaticResource PanelStyle}">
                            <StackPanel Margin="12">
                                <TextBlock Text="Tracking" Style="{StaticResource PanelHeaderStyle}" Margin="0,0,0,12"/>

                                <StackPanel Orientation="Horizontal">
                                    <Label Content="Completed Deals Variable" Style="{StaticResource LabelStyle}"/>
                                    <views:InfoIcon TooltipText="Variable name to track completed deals (e.g., 'dealer_completed_deals')"/>
                                </StackPanel>
                                <TextBox Text="{Binding SelectedNpc.DealerDefaults.CompletedDealsVariable, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource TextBoxStyle}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 6: Runtime & Dialogue -->
            <TabItem Header="Runtime &amp; Dialogue">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ChatProcessing"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,8,0"
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=TabItem}, Path=Foreground}"/>
                            <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>

                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid Margin="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="Runtime Settings" Style="{StaticResource PanelHeaderStyle}"/>
                                        <views:InfoIcon TooltipText="These toggles override runtime values on the generated NPC. Leave a toggle off to keep S1API or prefab defaults."/>
                                    </StackPanel>
                                    <TextBlock Text="Use these only when you want to override the default runtime behavior S1API would otherwise keep."
                                               Foreground="{StaticResource DimTextBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,12"/>

                                    <CheckBox Content="Set Aggressiveness" IsChecked="{Binding SelectedNpc.RuntimeSettings.SetAggressiveness}" Foreground="{StaticResource LightTextBrush}" Margin="0,0,0,8"/>
                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Aggressiveness" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Raw aggressiveness float used by the NPC runtime. Start around 0 to 1 unless you are deliberately pushing the behavior harder."/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.RuntimeSettings.Aggressiveness, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,8" IsEnabled="{Binding SelectedNpc.RuntimeSettings.SetAggressiveness}"/>

                                    <CheckBox Content="Set Region" IsChecked="{Binding SelectedNpc.RuntimeSettings.SetRegion}" Foreground="{StaticResource LightTextBrush}" Margin="0,0,0,8"/>
                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Region" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="Region gate and runtime region. Use one of the main region names such as Northtown, Downtown, or Docks."/>
                                    </StackPanel>
                                    <ComboBox Text="{Binding SelectedNpc.RuntimeSettings.Region, UpdateSourceTrigger=PropertyChanged}" IsEditable="True" Style="{StaticResource ComboBoxStyle}" Margin="0,0,0,8" IsEnabled="{Binding SelectedNpc.RuntimeSettings.SetRegion}">
                                        <ComboBoxItem Content="Northtown"/>
                                        <ComboBoxItem Content="Westville"/>
                                        <ComboBoxItem Content="Downtown"/>
                                        <ComboBoxItem Content="Docks"/>
                                        <ComboBoxItem Content="Suburbia"/>
                                        <ComboBoxItem Content="Uptown"/>
                                    </ComboBox>

                                    <CheckBox Content="Set Scale" IsChecked="{Binding SelectedNpc.RuntimeSettings.SetScale}" Foreground="{StaticResource LightTextBrush}" Margin="0,0,0,8"/>
                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Scale" Style="{StaticResource LabelStyle}"/>
                                        <views:InfoIcon TooltipText="1.0 is default size. Values above 1 make the NPC larger and values below 1 make the NPC smaller."/>
                                    </StackPanel>
                                    <TextBox Text="{Binding SelectedNpc.RuntimeSettings.Scale, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,8" IsEnabled="{Binding SelectedNpc.RuntimeSettings.SetScale}"/>

                                    <CheckBox Content="Override RequiresRegionUnlocked" IsChecked="{Binding SelectedNpc.RuntimeSettings.OverrideRequiresRegionUnlocked}" Foreground="{StaticResource LightTextBrush}" Margin="0,0,0,8"/>
                                    <StackPanel Orientation="Horizontal" Margin="16,0,0,0">
                                        <CheckBox Content="Requires Region Unlocked" IsChecked="{Binding SelectedNpc.RuntimeSettings.RequiresRegionUnlocked}" Foreground="{StaticResource LightTextBrush}" IsEnabled="{Binding SelectedNpc.RuntimeSettings.OverrideRequiresRegionUnlocked}"/>
                                        <views:InfoIcon TooltipText="Only used when the override above is enabled. If checked, the NPC stays gated until that region is unlocked in-game."/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="Prefab Behaviors" Style="{StaticResource PanelHeaderStyle}"/>
                                        <views:InfoIcon TooltipText="Adds built-in S1API prefab behaviours like smoke breaks, graffiti, drinking, and held items. Most path fields expect a Resources path to an AvatarEquippable or prefab."/>
                                    </StackPanel>
                                    <TextBlock Text="These options attach ready-made S1API behavior components. If a path is blank, S1API uses its fallback where available."
                                               Foreground="{StaticResource DimTextBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,12"/>

                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="Enable Smoke Break" IsChecked="{Binding SelectedNpc.RuntimeSettings.EnableSmokeBreak}" Foreground="{StaticResource LightTextBrush}" Margin="0,0,0,8"/>
                                        <views:InfoIcon TooltipText="Adds SmokeBreakBehaviour. Leave the cigarette path blank to let S1API try its fallback. Set a custom path when you ship your own lit cigarette prefab."/>
                                    </StackPanel>
                                    <StackPanel Margin="16,0,0,0">
                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="Cigarette Prefab Path (Optional)" Style="{StaticResource LabelStyle}"/>
                                            <views:InfoIcon TooltipText="Resources path to the cigarette prefab used while smoking. Example: 'MyMod/Cigarette_Lit'. Leave blank to let S1API try its built-in fallback."/>
                                        </StackPanel>
                                        <TextBox Text="{Binding SelectedNpc.RuntimeSettings.SmokeBreakCigarettePath, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,4" IsEnabled="{Binding SelectedNpc.RuntimeSettings.EnableSmokeBreak}"/>
                                        <TextBlock Text="Example: MyMod/Cigarette_Lit. Leave blank if you do not ship a custom cigarette prefab."
                                                   Foreground="{StaticResource DimTextBrush}"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,8"/>
                                        <StackPanel Orientation="Horizontal">
                                            <CheckBox Content="Smoke Break Debug Mode" IsChecked="{Binding SelectedNpc.RuntimeSettings.EnableSmokeBreakDebugMode}" Foreground="{StaticResource LightTextBrush}" Margin="0,0,0,8" IsEnabled="{Binding SelectedNpc.RuntimeSettings.EnableSmokeBreak}"/>
                                            <views:InfoIcon TooltipText="Turns on verbose smoke break logging. Only use this while testing because it is debug noise, not gameplay behavior."/>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="Enable Graffiti" IsChecked="{Binding SelectedNpc.RuntimeSettings.EnableGraffiti}" Foreground="{StaticResource LightTextBrush}" Margin="0,0,0,8"/>
                                        <views:InfoIcon TooltipText="Adds graffiti behaviour. If the path is blank S1API tries the built-in spray paint equippable automatically."/>
                                    </StackPanel>
                                    <StackPanel Margin="16,0,0,0">
                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="Spray Paint Equippable Path (Optional)" Style="{StaticResource LabelStyle}"/>
                                            <views:InfoIcon TooltipText="Resources path to the spray paint AvatarEquippable. Example: 'Weapons/SprayPaint/SprayPaint_AvatarEquippable'."/>
                                        </StackPanel>
                                        <TextBox Text="{Binding SelectedNpc.RuntimeSettings.SprayPaintEquippablePath, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,4" IsEnabled="{Binding SelectedNpc.RuntimeSettings.EnableGraffiti}"/>
                                        <TextBlock Text="Example: Weapons/SprayPaint/SprayPaint_AvatarEquippable"
                                                   Foreground="{StaticResource DimTextBrush}"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,8"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="Enable Drinking" IsChecked="{Binding SelectedNpc.RuntimeSettings.EnableDrinking}" Foreground="{StaticResource LightTextBrush}" Margin="0,0,0,8"/>
                                        <views:InfoIcon TooltipText="Adds drink behaviour. If the path is blank, S1API defaults to a base-game drink such as beer."/>
                                    </StackPanel>
                                    <StackPanel Margin="16,0,0,0">
                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="Drink Equippable Path (Optional)" Style="{StaticResource LabelStyle}"/>
                                            <views:InfoIcon TooltipText="Resources path to the drink AvatarEquippable. Examples: 'Avatar/Equippables/Beer' or 'Avatar/Equippables/Coffee'."/>
                                        </StackPanel>
                                        <TextBox Text="{Binding SelectedNpc.RuntimeSettings.DrinkEquippablePath, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,4" IsEnabled="{Binding SelectedNpc.RuntimeSettings.EnableDrinking}"/>
                                        <TextBlock Text="Example: Avatar/Equippables/Beer"
                                                   Foreground="{StaticResource DimTextBrush}"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,8"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="Enable Item Holding" IsChecked="{Binding SelectedNpc.RuntimeSettings.EnableItemHolding}" Foreground="{StaticResource LightTextBrush}" Margin="0,0,0,8"/>
                                        <views:InfoIcon TooltipText="Adds generic held-item behaviour. If the path is blank, S1API defaults to a lowered phone."/>
                                    </StackPanel>
                                    <StackPanel Margin="16,0,0,0">
                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="Held Item Equippable Path (Optional)" Style="{StaticResource LabelStyle}"/>
                                            <views:InfoIcon TooltipText="Resources path to the AvatarEquippable to hold. Examples: 'Avatar/Equippables/Phone_Lowered' or 'Tools/Flashlight/Flashlight_AvatarEquippable'."/>
                                        </StackPanel>
                                        <TextBox Text="{Binding SelectedNpc.RuntimeSettings.HeldItemEquippablePath, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,4" IsEnabled="{Binding SelectedNpc.RuntimeSettings.EnableItemHolding}"/>
                                        <TextBlock Text="Examples: Avatar/Equippables/Phone_Lowered, Tools/Flashlight/Flashlight_AvatarEquippable"
                                                   Foreground="{StaticResource DimTextBrush}"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,8"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <CheckBox Content="Generate Hook Scaffold" IsChecked="{Binding SelectedNpc.GenerateHookScaffold}" Foreground="{StaticResource LightTextBrush}"/>
                                        <views:InfoIcon TooltipText="Creates a partial hooks file in the exported mod so you can add advanced runtime code without editing generated files."/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource PanelStyle}">
                                <StackPanel Margin="12">
                                    <DockPanel Margin="0,0,0,12">
                                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                            <TextBlock Text="Dialogue Database" Style="{StaticResource PanelHeaderStyle}"/>
                                            <views:InfoIcon TooltipText="Defines dialogue text entries for this NPC. Each line in the Text box becomes one possible spoken variant in the generated database."/>
                                        </StackPanel>
                                        <Button Content="+ Add Entry" Style="{StaticResource IconButtonStyle}" Click="AddDialogueDatabaseEntry_Click" DockPanel.Dock="Right"/>
                                    </DockPanel>
                                    <TextBlock Text="Use this for reusable dialogue text keys. Module Name is optional, Key is the internal ID, and each text line becomes a possible variant."
                                               Foreground="{StaticResource DimTextBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,12"/>
                                    <ItemsControl ItemsSource="{Binding SelectedNpc.DialogueDatabaseEntries}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type models:NpcDialogueDatabaseEntryBlueprint}">
                                                <Border BorderBrush="{StaticResource DarkBorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,8">
                                                    <StackPanel>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Module Name (Optional)" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Optional grouping name. Leave blank for generic entries. Example: 'QuestIntro'."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding ModuleName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Key" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Internal lookup key used by dialogue containers or code. Keep it unique inside its module. Example: 'GREETING_01'."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Text" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Player-facing NPC line. Use multiple lines to generate multiple possible variants for the same key."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" AcceptsReturn="True" Height="72" TextWrapping="Wrap" Margin="0,0,0,6"/>
                                                        <Button Content="Remove Entry" Style="{StaticResource IconButtonStyle}" Click="RemoveDialogueDatabaseEntry_Click" Tag="{Binding}"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <DockPanel Margin="0,0,0,12">
                                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                            <TextBlock Text="Dialogue Containers" Style="{StaticResource PanelHeaderStyle}"/>
                                            <views:InfoIcon TooltipText="Build full conversation trees here. Container Name is the internal ID; Node Labels are the jump targets used by choices, callbacks, and event reactions."/>
                                        </StackPanel>
                                        <Button Content="+ Add Container" Style="{StaticResource IconButtonStyle}" Click="AddDialogueContainer_Click" DockPanel.Dock="Right"/>
                                    </DockPanel>
                                    <TextBlock Text="Use one container with UseOnInteract or UseOnInteractOnce when you want it to open during normal interaction. Node labels are internal jump targets, not player text."
                                               Foreground="{StaticResource DimTextBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,12"/>
                                    <ItemsControl ItemsSource="{Binding SelectedNpc.DialogueContainers}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type models:NpcDialogueContainerBlueprint}">
                                                <Expander Header="{Binding DisplayName}" Margin="0,0,0,8" IsExpanded="False">
                                                    <StackPanel Margin="8">
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Container Name" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Internal container ID. Use a stable name like 'ShopDialogue' or 'QuestIntro'. This must match exactly when other fields jump into the container."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <CheckBox Content="Allow Exit" IsChecked="{Binding AllowExit}" Foreground="{StaticResource LightTextBrush}" Margin="0,0,0,6"/>
                                                            <views:InfoIcon TooltipText="If disabled, the player cannot naturally back out of this container. Only scripted jumps or choices can end the conversation."/>
                                                        </StackPanel>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Interact Mode" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="None keeps the container registered but does not auto-open it. UseOnInteract opens it every time you talk. UseOnInteractOnce overrides the next interaction once, then returns to normal."/>
                                                        </StackPanel>
                                                        <ComboBox ItemsSource="{Binding Source={x:Type models:NpcDialogueInteractMode}, Converter={StaticResource EnumToCollectionConverter}}" SelectedItem="{Binding InteractMode}" Style="{StaticResource ComboBoxStyle}" Margin="0,0,0,8"/>
                                                        <Button Content="+ Add Node" Style="{StaticResource IconButtonStyle}" Click="AddDialogueNode_Click" Tag="{Binding}" Margin="0,0,0,8"/>
                                                        <ItemsControl ItemsSource="{Binding Nodes}">
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate DataType="{x:Type models:NpcDialogueNodeBlueprint}">
                                                                    <Border BorderBrush="{StaticResource DarkBorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,8">
                                                                        <StackPanel>
                                                                            <StackPanel Orientation="Horizontal">
                                                                                <Label Content="Node Label" Style="{StaticResource LabelStyle}"/>
                                                                                <views:InfoIcon TooltipText="Internal node ID used for jumps and callback matching. Examples: 'ENTRY', 'SHOP_MENU', or 'ASK_STATUS'."/>
                                                                            </StackPanel>
                                                                            <TextBox Text="{Binding NodeLabel, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                                            <StackPanel Orientation="Horizontal">
                                                                                <Label Content="Node Text" Style="{StaticResource LabelStyle}"/>
                                                                                <views:InfoIcon TooltipText="NPC text shown when this node is displayed."/>
                                                                            </StackPanel>
                                                                            <TextBox Text="{Binding NodeText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" AcceptsReturn="True" Height="72" TextWrapping="Wrap" Margin="0,0,0,6"/>
                                                                            <Button Content="+ Add Choice" Style="{StaticResource IconButtonStyle}" Click="AddDialogueChoice_Click" Tag="{Binding}" Margin="0,0,0,6"/>
                                                                            <ItemsControl ItemsSource="{Binding Choices}">
                                                                                <ItemsControl.ItemTemplate>
                                                                                    <DataTemplate DataType="{x:Type models:NpcDialogueChoiceBlueprint}">
                                                                                        <Border BorderBrush="{StaticResource DarkBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="8" Margin="0,0,0,6">
                                                                                            <StackPanel>
                                                                                                <StackPanel Orientation="Horizontal">
                                                                                                    <Label Content="Choice Label" Style="{StaticResource LabelStyle}"/>
                                                                                                    <views:InfoIcon TooltipText="Internal choice ID used by OnChoiceSelected callbacks. Keep it unique inside the container. Example: 'ASK_JOB'."/>
                                                                                                </StackPanel>
                                                                                                <TextBox Text="{Binding ChoiceLabel, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                                                                <StackPanel Orientation="Horizontal">
                                                                                                    <Label Content="Choice Text" Style="{StaticResource LabelStyle}"/>
                                                                                                    <views:InfoIcon TooltipText="Player-facing text shown for this choice."/>
                                                                                                </StackPanel>
                                                                                                <TextBox Text="{Binding ChoiceText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                                                                <StackPanel Orientation="Horizontal">
                                                                                                    <Label Content="Target Node Label (Optional)" Style="{StaticResource LabelStyle}"/>
                                                                                                    <views:InfoIcon TooltipText="Node label to jump to after the player selects this choice. Leave blank for a terminal or callback-only option."/>
                                                                                                </StackPanel>
                                                                                                <TextBox Text="{Binding TargetNodeLabel, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                                                                <Button Content="Remove Choice" Style="{StaticResource IconButtonStyle}" Click="RemoveDialogueChoice_Click" Tag="{Binding}"/>
                                                                                            </StackPanel>
                                                                                        </Border>
                                                                                    </DataTemplate>
                                                                                </ItemsControl.ItemTemplate>
                                                                            </ItemsControl>
                                                                            <Button Content="Remove Node" Style="{StaticResource IconButtonStyle}" Click="RemoveDialogueNode_Click" Tag="{Binding}"/>
                                                                        </StackPanel>
                                                                    </Border>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                        <Button Content="Remove Container" Style="{StaticResource IconButtonStyle}" Click="RemoveDialogueContainer_Click" Tag="{Binding}"/>
                                                    </StackPanel>
                                                </Expander>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <DockPanel Margin="0,0,0,12">
                                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                            <TextBlock Text="Dialogue Callbacks" Style="{StaticResource PanelHeaderStyle}"/>
                                            <views:InfoIcon TooltipText="Run simple generated actions when dialogue events happen. Match Key uses a choice label for ChoiceSelected, a node label for NodeDisplayed, and is ignored for ConversationStarted."/>
                                        </StackPanel>
                                        <Button Content="+ Add Callback" Style="{StaticResource IconButtonStyle}" Click="AddDialogueCallback_Click" DockPanel.Dock="Right"/>
                                    </DockPanel>
                                    <TextBlock Text="Callbacks can send a text message, jump into another container and node, or stop the current override. Use hook scaffolds for anything more complex than that."
                                               Foreground="{StaticResource DimTextBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,12"/>
                                    <ItemsControl ItemsSource="{Binding SelectedNpc.DialogueCallbacks}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type models:NpcDialogueCallbackBlueprint}">
                                                <Border BorderBrush="{StaticResource DarkBorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,8">
                                                    <StackPanel>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Callback Type" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="ChoiceSelected watches a choice label, NodeDisplayed watches a node label, and ConversationStarted fires as soon as the conversation begins."/>
                                                        </StackPanel>
                                                        <ComboBox ItemsSource="{Binding Source={x:Type models:NpcDialogueCallbackType}, Converter={StaticResource EnumToCollectionConverter}}" SelectedItem="{Binding CallbackType}" Style="{StaticResource ComboBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Match Key" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="For ChoiceSelected use a choice label. For NodeDisplayed use a node label. ConversationStarted ignores this field."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding MatchKey, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Message Text (Optional)" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Optional SMS-style text message to send when this callback fires."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" AcceptsReturn="True" Height="60" TextWrapping="Wrap" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Jump To Container Name (Optional)" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Optional container to jump into when the callback fires. Fill both jump fields if you want a dialogue jump."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding JumpToContainerName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Jump To Node Label (Optional)" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Node label inside the target container to jump to. This is a node label, not a GUID."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding JumpToNodeLabel, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <CheckBox Content="Stop Dialogue Override" IsChecked="{Binding StopDialogueOverride}" Foreground="{StaticResource LightTextBrush}" Margin="0,0,0,6"/>
                                                            <views:InfoIcon TooltipText="Stops the current dialogue override before the rest of the generated action runs. Safe for choice callbacks. Avoid enabling this on NodeDisplayed because it can recurse."/>
                                                        </StackPanel>
                                                        <DockPanel Margin="0,0,0,6">
                                                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                                                <TextBlock Text="Global Save Setters" FontWeight="SemiBold" Foreground="{StaticResource LightTextBrush}"/>
                                                                <views:InfoIcon TooltipText="Writes into generated Global Save Variables when this callback fires. Values are authored as text and parsed into the selected save-field type at runtime." Margin="8,0,0,0"/>
                                                            </StackPanel>
                                                            <Button Content="+ Add Setter"
                                                                    Style="{StaticResource IconButtonStyle}"
                                                                    Click="AddGeneratedActionGlobalStateSetter_Click"
                                                                    Tag="{Binding}"
                                                                    DockPanel.Dock="Right"/>
                                                        </DockPanel>
                                                        <ItemsControl ItemsSource="{Binding GlobalStateSetters}" ItemTemplate="{StaticResource NpcGlobalStateSetterTemplate}"/>
                                                        <Button Content="Remove Callback" Style="{StaticResource IconButtonStyle}" Click="RemoveDialogueCallback_Click" Tag="{Binding}"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                                <StackPanel Margin="12">
                                    <DockPanel Margin="0,0,0,12">
                                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                            <TextBlock Text="Dialogue Injections" Style="{StaticResource PanelHeaderStyle}"/>
                                            <views:InfoIcon TooltipText="Adds one extra choice into an existing vanilla or custom dialogue container. Container name and node GUIDs must match the target NPC's live dialogue exactly."/>
                                        </StackPanel>
                                        <Button Content="+ Add Injection" Style="{StaticResource IconButtonStyle}" Click="AddDialogueInjection_Click" DockPanel.Dock="Right"/>
                                    </DockPanel>
                                    <TextBlock Text="This is advanced dialogue work. GUID fields are not node labels; use injections only when you know the exact target container and node GUIDs you want to patch."
                                               Foreground="{StaticResource DimTextBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,12"/>
                                    <ItemsControl ItemsSource="{Binding SelectedNpc.DialogueInjections}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type models:NpcDialogueInjectionBlueprint}">
                                                <Border BorderBrush="{StaticResource DarkBorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,8">
                                                    <StackPanel>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Target NPC" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="NPC ID to inject into. Leave blank to target this generated NPC. Use the exact NPC ID when patching a vanilla or another custom NPC."/>
                                                        </StackPanel>
                                                        <ComboBox ItemsSource="{Binding ElementName=RootControl, Path=AvailableNpcs}"
                                                                  DisplayMemberPath="DisplayName"
                                                                  SelectedValuePath="Id"
                                                                  SelectedValue="{Binding TargetNpcId}"
                                                                  IsEditable="True"
                                                                  Style="{StaticResource ComboBoxStyle}"
                                                                  Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Container Name" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Exact dialogue container name on the target NPC. Example: 'CasinoDialogue'. The match must be exact."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding ContainerName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="From Node GUID" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="GUID of the existing source node that should receive the new choice. This is not a node label."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding FromNodeGuid, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="To Node GUID" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="GUID of the existing destination node the new choice should link to. That target node must already exist in the container."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding ToNodeGuid, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                        <TextBlock Text="Tip: container and GUID mismatches are skipped silently by S1API, so double-check these values."
                                                                   Foreground="{StaticResource DimTextBrush}"
                                                                   TextWrapping="Wrap"
                                                                   Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Choice Label" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Internal unique ID for the injected choice callback. Make it mod-specific, for example 'MYMOD_ASK_PAYOUTS'."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding ChoiceLabel, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Choice Text" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Player-facing text shown for the injected choice."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding ChoiceText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Message Text (Optional)" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Optional SMS-style text message to send when the injected choice is confirmed."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" AcceptsReturn="True" Height="60" TextWrapping="Wrap" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Jump To Container Name (Optional)" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Optional container to jump into after the injected choice is confirmed. Fill both jump fields if you want a dialogue jump."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding JumpToContainerName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Jump To Node Label (Optional)" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Node label inside the target container to jump to after confirmation. This field uses a node label, not a GUID."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding JumpToNodeLabel, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <CheckBox Content="Stop Dialogue Override" IsChecked="{Binding StopDialogueOverride}" Foreground="{StaticResource LightTextBrush}" Margin="0,0,0,6"/>
                                                            <views:InfoIcon TooltipText="Stops the current override on the target NPC before any generated message or jump runs."/>
                                                        </StackPanel>
                                                        <DockPanel Margin="0,0,0,6">
                                                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                                                <TextBlock Text="Global Save Setters" FontWeight="SemiBold" Foreground="{StaticResource LightTextBrush}"/>
                                                                <views:InfoIcon TooltipText="Writes into generated Global Save Variables after this injection runs. Useful for conversation flags, once-only gates, or cross-system state handoff." Margin="8,0,0,0"/>
                                                            </StackPanel>
                                                            <Button Content="+ Add Setter"
                                                                    Style="{StaticResource IconButtonStyle}"
                                                                    Click="AddGeneratedActionGlobalStateSetter_Click"
                                                                    Tag="{Binding}"
                                                                    DockPanel.Dock="Right"/>
                                                        </DockPanel>
                                                        <ItemsControl ItemsSource="{Binding GlobalStateSetters}" ItemTemplate="{StaticResource NpcGlobalStateSetterTemplate}"/>
                                                        <Button Content="Remove Injection" Style="{StaticResource IconButtonStyle}" Click="RemoveDialogueInjection_Click" Tag="{Binding}"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource PanelStyle}">
                                <StackPanel Margin="12">
                                    <DockPanel Margin="0,0,0,12">
                                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                            <TextBlock Text="Runtime Event Reactions" Style="{StaticResource PanelHeaderStyle}"/>
                                            <views:InfoIcon TooltipText="Runs simple generated actions when S1API runtime events fire. Customer and dealer events only fire if this NPC has those systems enabled."/>
                                        </StackPanel>
                                        <Button Content="+ Add Reaction" Style="{StaticResource IconButtonStyle}" Click="AddEventReaction_Click" DockPanel.Dock="Right"/>
                                    </DockPanel>
                                    <TextBlock Text="Use reactions for lightweight messages or dialogue jumps when customer, dealer, or relationship events happen. Use hooks if you need custom runtime code."
                                               Foreground="{StaticResource DimTextBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,12"/>
                                    <ItemsControl ItemsSource="{Binding SelectedNpc.EventReactions}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type models:NpcRuntimeEventReactionBlueprint}">
                                                <Border BorderBrush="{StaticResource DarkBorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,8">
                                                    <StackPanel>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Event Type" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Pick the runtime event that should trigger this generated action. Customer and dealer events require those systems to be enabled on this NPC."/>
                                                        </StackPanel>
                                                        <ComboBox ItemsSource="{Binding Source={x:Type models:NpcRuntimeEventType}, Converter={StaticResource EnumToCollectionConverter}}" SelectedItem="{Binding EventType}" Style="{StaticResource ComboBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Message Text (Optional)" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Optional SMS-style text message sent when the event fires."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" AcceptsReturn="True" Height="60" TextWrapping="Wrap" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Jump To Container Name (Optional)" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Optional dialogue container to jump into when the event fires. Fill both jump fields if you want a dialogue jump."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding JumpToContainerName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Label Content="Jump To Node Label (Optional)" Style="{StaticResource LabelStyle}"/>
                                                            <views:InfoIcon TooltipText="Node label inside the target container to jump to. This field uses a node label, not a GUID."/>
                                                        </StackPanel>
                                                        <TextBox Text="{Binding JumpToNodeLabel, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,6"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <CheckBox Content="Stop Dialogue Override" IsChecked="{Binding StopDialogueOverride}" Foreground="{StaticResource LightTextBrush}" Margin="0,0,0,6"/>
                                                            <views:InfoIcon TooltipText="Stops the current dialogue override before the generated message or jump runs."/>
                                                        </StackPanel>
                                                        <DockPanel Margin="0,0,0,6">
                                                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                                                <TextBlock Text="Global Save Setters" FontWeight="SemiBold" Foreground="{StaticResource LightTextBrush}"/>
                                                                <views:InfoIcon TooltipText="Writes into generated Global Save Variables when this runtime event fires. Use it for counters, unlock flags, or relationship/dealer/customer state handoff." Margin="8,0,0,0"/>
                                                            </StackPanel>
                                                            <Button Content="+ Add Setter"
                                                                    Style="{StaticResource IconButtonStyle}"
                                                                    Click="AddGeneratedActionGlobalStateSetter_Click"
                                                                    Tag="{Binding}"
                                                                    DockPanel.Dock="Right"/>
                                                        </DockPanel>
                                                        <ItemsControl ItemsSource="{Binding GlobalStateSetters}" ItemTemplate="{StaticResource NpcGlobalStateSetterTemplate}"/>
                                                        <Button Content="Remove Reaction" Style="{StaticResource IconButtonStyle}" Click="RemoveEventReaction_Click" Tag="{Binding}"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 7: Inventory -->
            <TabItem Header="Inventory">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Backpack" 
                                                   VerticalAlignment="Center" 
                                                   Margin="0,0,8,0"
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=TabItem}, Path=Foreground}"/>
                            <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>

                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="12" MaxWidth="600" HorizontalAlignment="Left">
                        <!-- Startup Items -->
                        <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                            <StackPanel Margin="12">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <TextBlock Text="Startup Items" Style="{StaticResource PanelHeaderStyle}"/>
                                    <views:InfoIcon TooltipText="Items that will always be in inventory when NPC spawns (e.g., 'banana', 'baseballbat')"/>
                                </StackPanel>

                                <ListBox x:Name="StartupItemsListBox"
                                         ItemsSource="{Binding SelectedNpc.InventoryDefaults.StartupItems}"
                                         Height="150"
                                         Background="{StaticResource DarkBackgroundBrush}"
                                         Foreground="{StaticResource LightTextBrush}"
                                         BorderBrush="{StaticResource DarkBorderBrush}"
                                         Margin="0,0,0,4"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ComboBox x:Name="StartupItemComboBox"
                                              Grid.Column="0"
                                              ItemsSource="{Binding AvailableItemReferences}"
                                              DisplayMemberPath="DisplayLabel"
                                              Style="{StaticResource ComboBoxStyle}"
                                              IsEditable="True"
                                              Margin="0,0,4,0"/>
                                    <Button Grid.Column="1" Content="Add"
                                            Style="{StaticResource IconButtonStyle}"
                                            Click="AddStartupItem_Click"
                                            Margin="0,0,4,0"/>
                                    <Button Grid.Column="2" Content="Remove"
                                            Style="{StaticResource IconButtonStyle}"
                                            Click="RemoveStartupItem_Click"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Cash Settings -->
                        <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,12">
                            <StackPanel Margin="12">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <TextBlock Text="Cash Settings" Style="{StaticResource PanelHeaderStyle}"/>
                                    <views:InfoIcon TooltipText="NPC will spawn with random cash between min and max"/>
                                </StackPanel>

                                <CheckBox Content="Enable Random Cash"
                                          IsChecked="{Binding SelectedNpc.InventoryDefaults.EnableRandomCash}"
                                          Foreground="{StaticResource LightTextBrush}" Margin="0,0,0,8"/>

                                <StackPanel Orientation="Horizontal">
                                    <Label Content="Min Cash Amount ($)" Style="{StaticResource LabelStyle}"/>
                                    <views:InfoIcon TooltipText="Minimum cash amount ($) NPC will spawn with (randomized between min and max)"/>
                                </StackPanel>
                                <TextBox Text="{Binding SelectedNpc.InventoryDefaults.RandomCashMin, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource TextBoxStyle}"
                                         IsEnabled="{Binding SelectedNpc.InventoryDefaults.EnableRandomCash}"/>

                                <StackPanel Orientation="Horizontal">
                                    <Label Content="Max Cash Amount ($)" Style="{StaticResource LabelStyle}"/>
                                    <views:InfoIcon TooltipText="Maximum cash amount ($) NPC will spawn with (randomized between min and max)"/>
                                </StackPanel>
                                <TextBox Text="{Binding SelectedNpc.InventoryDefaults.RandomCashMax, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource TextBoxStyle}"
                                         IsEnabled="{Binding SelectedNpc.InventoryDefaults.EnableRandomCash}"/>
                            </StackPanel>
                        </Border>

                        <!-- Persistence -->
                        <Border Style="{StaticResource PanelStyle}">
                            <StackPanel Margin="12">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <TextBlock Text="Persistence" Style="{StaticResource PanelHeaderStyle}"/>
                                    <views:InfoIcon TooltipText="Controls whether inventory is preserved across sleep cycles"/>
                                </StackPanel>

                                <CheckBox Content="Clear Inventory Each Night"
                                          IsChecked="{Binding SelectedNpc.InventoryDefaults.ClearInventoryEachNight}"
                                          Foreground="{StaticResource LightTextBrush}"/>
                                <TextBlock Text="If unchecked, NPC keeps items between days. If checked, inventory resets each sleep."
                                           Foreground="{StaticResource DimTextBrush}"
                                           FontSize="11"
                                           TextWrapping="Wrap"
                                           Margin="24,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 8: Preview NPC -->
            <TabItem x:Name="PreviewNpcTab" Header="Preview NPC">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="PlayCircle" 
                                                   VerticalAlignment="Center" 
                                                   Margin="0,0,8,0"
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=TabItem}, Path=Foreground}"/>
                            <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>

                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="12" MaxWidth="600" HorizontalAlignment="Left">
                        <Border Style="{StaticResource PanelStyle}">
                            <StackPanel Margin="12">
                                <TextBlock Text="Preview NPC in Game" Style="{StaticResource PanelHeaderStyle}" Margin="0,0,0,12"/>
                                <TextBlock Text="Click the Launch Preview button to launch the game with appearance preview enabled. Changes to NPC appearance will be reflected in real-time in the game menu. Ensure the game is closed before launching the preview." 
                                           Foreground="{StaticResource DimTextBrush}"
                                           FontSize="11"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,12"/>
                                <Button Content="Launch Preview" 
                                        Command="{Binding PreviewNpcCommand}"
                                        Style="{StaticResource BaseButtonStyle}"
                                        Foreground="{StaticResource AccentOrangeBrush}"
                                        HorizontalAlignment="Left"
                                        Padding="20,8"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
