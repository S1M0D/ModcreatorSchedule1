<Window x:Class="Schedule1ModdingTool.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="700" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource DarkBackgroundBrush}"
        Foreground="{StaticResource LightTextBrush}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        RenderOptions.BitmapScalingMode="HighQuality"
        RenderOptions.ClearTypeHint="Enabled"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">
	<Border Margin="10"
	        Background="{StaticResource DarkPanelBrush}"
	        BorderBrush="{StaticResource DarkBorderBrush}"
	        BorderThickness="1"
	        CornerRadius="6"
	        Padding="15">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<TextBlock Grid.Row="0" Text="Mod Generation Settings" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />

			<ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
				<StackPanel Orientation="Vertical" Margin="0,0,8,0">
				<GroupBox Header="Workspace" Margin="0,0,0,8">
					<StackPanel Margin="5">
						<TextBlock Text="Default Workspace Path:" Margin="0,0,0,5" />
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="Auto" />
							</Grid.ColumnDefinitions>
							<TextBox Grid.Column="0" Text="{Binding Settings.WorkspacePath, UpdateSourceTrigger=PropertyChanged}"
							         Margin="0,0,5,0" />
							<Button Grid.Column="1" Content="Browse..." Command="{Binding BrowseWorkspacePathCommand}" Padding="10,0" />
						</Grid>
					</StackPanel>
				</GroupBox>

				<GroupBox Header="Game Installation" Margin="0,0,0,8">
					<StackPanel Margin="5">
						<TextBlock Text="Schedule I game folder (launch and deploy):" Margin="0,0,0,5" />
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="Auto" />
							</Grid.ColumnDefinitions>
							<TextBox Grid.Column="0" Text="{Binding Settings.GameInstallPath, UpdateSourceTrigger=PropertyChanged}"
							         Margin="0,0,5,0" />
							<Button Grid.Column="1" Content="Browse..." Command="{Binding BrowseGamePathCommand}" Padding="10,0" />
						</Grid>
						<TextBlock Text="Mono game assemblies (for generated mod builds):" Margin="0,10,0,5" />
						<DockPanel>
							<Button DockPanel.Dock="Right" Content="Browse..." Command="{Binding BrowseManagedAssembliesPathCommand}" Padding="10,0" Margin="5,0,0,0" />
							<TextBox Text="{Binding Settings.ManagedAssembliesPath, UpdateSourceTrigger=PropertyChanged}" />
						</DockPanel>
						<TextBlock Text="Select Schedule I_Data\Managed from a Mono install if the selected game folder has no Unity DLLs. Leave blank to search automatically."
						           TextWrapping="Wrap" Foreground="{StaticResource DimTextBrush}" FontSize="11" Margin="0,5,0,0" />
						<TextBlock Text="IL2CPP game assemblies (for IL2CPP builds):" Margin="0,10,0,5" />
						<DockPanel>
							<Button DockPanel.Dock="Right" Content="Browse..." Command="{Binding BrowseIl2CppAssembliesPathCommand}" Padding="10,0" Margin="5,0,0,0" />
							<TextBox Text="{Binding Settings.Il2CppAssembliesPath, UpdateSourceTrigger=PropertyChanged}" />
						</DockPanel>
						<TextBlock Text="Select MelonLoader\Il2CppAssemblies from an IL2CPP install. Leave blank to search the selected game folder."
						           TextWrapping="Wrap" Foreground="{StaticResource DimTextBrush}" FontSize="11" Margin="0,5,0,0" />
						<TextBlock Text="Build target:" Margin="0,12,0,5" />
						<StackPanel Orientation="Horizontal">
							<RadioButton Content="Mono" GroupName="BuildTarget" Margin="0,0,16,0" IsChecked="{Binding Settings.BuildTarget, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Mono}" />
							<RadioButton Content="IL2CPP" GroupName="BuildTarget" Margin="0,0,16,0" IsChecked="{Binding Settings.BuildTarget, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Il2Cpp}" />
							<RadioButton Content="Mono + IL2CPP" GroupName="BuildTarget" IsChecked="{Binding Settings.BuildTarget, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Both}" />
						</StackPanel>
						<TextBlock Text="Mono builds use only Mono references; IL2CPP builds use only IL2CPP references. Both creates separate DLLs."
						           TextWrapping="Wrap" Foreground="{StaticResource DimTextBrush}" FontSize="11" Margin="0,5,0,0" />
						<TextBlock Text="Preview connector folder (optional):" Margin="0,10,0,5" />
						<DockPanel>
							<Button DockPanel.Dock="Right" Content="Browse..." Command="{Binding BrowseConnectorFolderPathCommand}" Padding="10,0" Margin="5,0,0,0" />
							<TextBox Text="{Binding Settings.ConnectorFolderPath, UpdateSourceTrigger=PropertyChanged}" />
						</DockPanel>
						<TextBlock Text="Select the folder containing ModCreatorConnector.dll if preview cannot find its bundled connector."
						           TextWrapping="Wrap" Foreground="{StaticResource DimTextBrush}" FontSize="11" Margin="0,5,0,0" />
					</StackPanel>
				</GroupBox>

				<GroupBox Header="S1API Reference" Margin="0,0,0,8">
					<StackPanel Margin="5">
						<TextBlock Text="Custom S1API.dll (optional, overrides NuGet package):" Margin="0,0,0,5" />
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="Auto" />
							</Grid.ColumnDefinitions>
							<TextBox Grid.Column="0"
							         Text="{Binding Settings.S1ApiDllPath, UpdateSourceTrigger=PropertyChanged}"
							         Margin="0,0,5,0" />
							<Button Grid.Column="1"
							        Content="Browse..."
							        Command="{Binding BrowseS1ApiDllCommand}"
							        Padding="10,0" />
						</Grid>
						<TextBlock Margin="0,8,0,0"
						           Text="Leave blank to use S1API.Forked 3.2.0 from NuGet."
						           TextWrapping="Wrap"
						           Foreground="{StaticResource DimTextBrush}"
						           FontSize="11" />
					</StackPanel>
				</GroupBox>

				<GroupBox Header="User Preferences" Margin="0,0,0,8">
					<StackPanel Margin="5">
						<TextBlock Text="Coding Experience Level:" Margin="0,0,0,10" />
						<StackPanel>
							<RadioButton Content="No coding experience" 
							             IsChecked="{Binding Settings.ExperienceLevel, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=NoCodingExperience}"
							             Margin="0,5"
							             GroupName="ExperienceLevel" />
							<RadioButton Content="Some coding experience" 
							             IsChecked="{Binding Settings.ExperienceLevel, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=SomeCoding}"
							             Margin="0,5"
							             GroupName="ExperienceLevel" />
							<RadioButton Content="Experienced coder" 
							             IsChecked="{Binding Settings.ExperienceLevel, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=ExperiencedCoder}"
							             Margin="0,5"
							             GroupName="ExperienceLevel" />
						</StackPanel>
						<TextBlock Text="This affects whether the code view is shown by default." 
						           Margin="0,10,0,0"
						           Foreground="{StaticResource DimTextBrush}"
						           FontSize="11"
						           TextWrapping="Wrap" />
						
						<Grid Margin="0,15,0,0">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="Auto" />
							</Grid.ColumnDefinitions>
							<TextBlock Grid.Column="0" Text="Undo History Size:" VerticalAlignment="Center" Margin="0,0,10,0" />
							<Slider Grid.Column="1" 
							        Minimum="1" 
							        Maximum="50" 
							        TickFrequency="1" 
							        IsSnapToTickEnabled="True"
							        Value="{Binding Settings.UndoHistorySize, UpdateSourceTrigger=PropertyChanged}"
							        VerticalAlignment="Center"
							        Margin="0,0,10,0" />
							<TextBlock Grid.Column="2" 
							           Text="{Binding Settings.UndoHistorySize, StringFormat='{}{0} steps'}" 
							           VerticalAlignment="Center"
							           MinWidth="60" />
						</Grid>
						<TextBlock Text="Number of undo/redo steps to keep in history (1-50). Default: 5." 
						           Margin="0,5,0,0"
						           Foreground="{StaticResource DimTextBrush}"
						           FontSize="11"
						           TextWrapping="Wrap" />
						
						<TextBlock Text="Update Channel:" Margin="0,15,0,10" />
						<StackPanel>
							<RadioButton Content="Stable - Only receive stable releases" 
							             IsChecked="{Binding Settings.UpdateChannel, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Stable}"
							             Margin="0,5"
							             GroupName="UpdateChannel" />
							<RadioButton Content="Beta - Receive beta and stable releases" 
							             IsChecked="{Binding Settings.UpdateChannel, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Beta}"
							             Margin="0,5"
							             GroupName="UpdateChannel" />
						</StackPanel>
						<TextBlock Text="Beta channel users receive updates more frequently but may encounter bugs. Stable channel users only receive tested releases." 
						           Margin="0,5,0,0"
						           Foreground="{StaticResource DimTextBrush}"
						           FontSize="11"
						           TextWrapping="Wrap" />
					</StackPanel>
				</GroupBox>

				<GroupBox Header="Default Mod Settings">
					<StackPanel Margin="5">
						<Grid Margin="0,0,0,5">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							<TextBlock Grid.Column="0" Text="Default Namespace:" VerticalAlignment="Center" Margin="0,0,10,0" />
							<TextBox Grid.Column="1" Text="{Binding Settings.DefaultModNamespace, UpdateSourceTrigger=PropertyChanged}" />
						</Grid>
						<Grid Margin="0,0,0,5">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							<TextBlock Grid.Column="0" Text="Default Author:" VerticalAlignment="Center" Margin="0,0,10,0" />
							<TextBox Grid.Column="1" Text="{Binding Settings.DefaultModAuthor, UpdateSourceTrigger=PropertyChanged}" />
						</Grid>
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							<TextBlock Grid.Column="0" Text="Default Version:" VerticalAlignment="Center" Margin="0,0,10,0" />
							<TextBox Grid.Column="1" Text="{Binding Settings.DefaultModVersion, UpdateSourceTrigger=PropertyChanged}" />
						</Grid>
					</StackPanel>
				</GroupBox>
				</StackPanel>
			</ScrollViewer>

			<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
				<Button Content="Save" Command="{Binding SaveCommand}" Padding="15,5" Margin="0,0,10,0" MinWidth="75" />
				<Button Content="Cancel" Command="{Binding CancelCommand}" Padding="15,5" MinWidth="75" />
			</StackPanel>
		</Grid>
	</Border>
</Window>
