<UserControl x:Class="Schedule1ModdingTool.Views.BlueprintControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="200">
    
    <Border Style="{StaticResource BlueprintItemStyle}" 
            MouseLeftButtonDown="Border_MouseLeftButtonDown"
            AllowDrop="False">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="📋" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource LightTextBrush}"/>
            <StackPanel>
                <TextBlock Text="{Binding QuestTitle}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource LightTextBrush}"/>
                <TextBlock Text="{Binding ClassName}" FontSize="10" Foreground="{StaticResource DimTextBrush}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
