<UserControl x:Class="Schedule1ModdingTool.Views.Controls.DrugAffinityEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:Schedule1ModdingTool.Models"
             xmlns:views="clr-namespace:Schedule1ModdingTool.Views"
             mc:Ignorable="d"
             d:DesignHeight="250" d:DesignWidth="450">
    <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <Label Content="Drug Affinities" Style="{StaticResource LabelStyle}"/>
            <views:InfoIcon TooltipText="Drug preferences: Positive values = likes, Negative = dislikes, 0 = neutral. Range: -1.0 to 1.0"/>
        </StackPanel>

        <!-- Drug Affinities List -->
        <ItemsControl ItemsSource="{Binding DrugAffinities, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="0,0,0,8">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type models:DrugAffinity}">
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <ComboBox Grid.Column="0"
                                  SelectedValue="{Binding DrugType}"
                                  SelectedValuePath="Content"
                                  Style="{StaticResource ComboBoxStyle}"
                                  Margin="0,0,8,0">
                            <ComboBoxItem Content="Marijuana"/>
                            <ComboBoxItem Content="Methamphetamine"/>
                            <ComboBoxItem Content="Cocaine"/>
                            <ComboBoxItem Content="MDMA"/>
                            <ComboBoxItem Content="Shrooms"/>
                            <ComboBoxItem Content="Heroin"/>
                        </ComboBox>

                        <TextBox Grid.Column="1"
                                 Text="{Binding AffinityValue, UpdateSourceTrigger=LostFocus, StringFormat=F2}"
                                 Style="{StaticResource TextBoxStyle}"
                                 Margin="0,0,8,0"/>

                        <Button Grid.Column="2"
                                Content="✕"
                                Style="{StaticResource IconButtonStyle}"
                                Padding="8,2"
                                Tag="{Binding}"
                                Click="RemoveAffinity_Click"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Content="+ Add Drug Affinity"
                Style="{StaticResource IconButtonStyle}"
                Click="AddAffinity_Click"/>
    </StackPanel>
</UserControl>
