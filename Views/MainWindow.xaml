<Window x:Class="Schedule1ModdingTool.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:Schedule1ModdingTool.Views"
        xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
        xmlns:utils="clr-namespace:Schedule1ModdingTool.Utils"
        mc:Ignorable="d"
        Title="{Binding CurrentProject.DisplayTitle, StringFormat='Schedule 1 Modding Tool - {0}'}"
        Height="800" Width="1200"
        MinHeight="600" MinWidth="800"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="SplitterStyle" TargetType="GridSplitter">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Menu Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <MenuItem Header="_File">
                <MenuItem Header="_New Project" Command="{Binding NewProjectCommand}" InputGestureText="Ctrl+N">
                    <MenuItem.Icon>
                        <TextBlock Text="📄" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Open Project..." Command="{Binding OpenProjectCommand}" InputGestureText="Ctrl+O">
                    <MenuItem.Icon>
                        <TextBlock Text="📂" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Save Project" Command="{Binding SaveProjectCommand}" InputGestureText="Ctrl+S">
                    <MenuItem.Icon>
                        <TextBlock Text="💾" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Save Project _As..." Command="{Binding SaveProjectAsCommand}" InputGestureText="Ctrl+Shift+S">
                    <MenuItem.Icon>
                        <TextBlock Text="💾" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="E_xit" Command="{Binding ExitCommand}" InputGestureText="Alt+F4">
                    <MenuItem.Icon>
                        <TextBlock Text="❌" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_Quest">
                <MenuItem Header="_Add Quest" Command="{Binding AddQuestCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="➕" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Remove Quest" Command="{Binding RemoveQuestCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="➖" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Regenerate Code" Command="{Binding RegenerateCodeCommand}" InputGestureText="F5">
                    <MenuItem.Icon>
                        <TextBlock Text="🔄" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_Build">
                <MenuItem Header="_Export Mod Project" Command="{Binding ExportModProjectCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="📦" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Build Mod" Command="{Binding BuildModCommand}" InputGestureText="F6">
                    <MenuItem.Icon>
                        <TextBlock Text="⚙️" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="Toggle _Code View" Command="{Binding ToggleCodeViewCommand}" IsCheckable="True" IsChecked="{Binding IsCodeVisible}">
                    <MenuItem.Icon>
                        <TextBlock Text="👁️" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_Tools">
                <MenuItem Header="_Settings" Command="{Binding OpenSettingsCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="⚙️" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>

        <!-- Toolbar -->
        <ToolBar Grid.Row="1" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Button Command="{Binding NewProjectCommand}" ToolTip="New Project">
                <TextBlock Text="📄" FontSize="16"/>
            </Button>
            <Button Command="{Binding OpenProjectCommand}" ToolTip="Open Project">
                <TextBlock Text="📂" FontSize="16"/>
            </Button>
            <Button Command="{Binding SaveProjectCommand}" ToolTip="Save Project">
                <TextBlock Text="💾" FontSize="16"/>
            </Button>
            <Separator/>
            <Button Command="{Binding AddQuestCommand}" ToolTip="Add Quest">
                <TextBlock Text="➕" FontSize="16"/>
            </Button>
            <Button Command="{Binding RemoveQuestCommand}" ToolTip="Remove Quest">
                <TextBlock Text="➖" FontSize="16"/>
            </Button>
            <Separator/>
            <Button Command="{Binding RegenerateCodeCommand}" ToolTip="Regenerate Code (F5)">
                <TextBlock Text="🔄" FontSize="16"/>
            </Button>
            <Separator/>
            <Button Command="{Binding ExportModProjectCommand}" ToolTip="Export Mod Project">
                <TextBlock Text="📦" FontSize="16"/>
            </Button>
            <Button Command="{Binding BuildModCommand}" ToolTip="Build Mod (F6)">
                <TextBlock Text="⚙️" FontSize="16"/>
            </Button>
        </ToolBar>

        <!-- Main Content Grid -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" MinWidth="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" MinWidth="300"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="300" MinWidth="250"/>
            </Grid.ColumnDefinitions>

            <!-- Resource Explorer Panel -->
            <Border Grid.Column="0" Style="{StaticResource PanelStyle}">
                <DockPanel>
                    <TextBlock Text="Resource Explorer" Style="{StaticResource PanelHeaderStyle}" DockPanel.Dock="Top"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="8">
                            <TextBlock Text="Blueprints" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <ItemsControl ItemsSource="{Binding AvailableBlueprints}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <views:BlueprintControl Blueprint="{Binding}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <!-- Vertical Splitter -->
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Style="{StaticResource SplitterStyle}"/>

            <!-- Workspace Panel -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto" x:Name="CodeViewRow"/>
                    <RowDefinition Height="*" x:Name="CodeContentRow"/>
                </Grid.RowDefinitions>

                <!-- Quest Workspace -->
                <Border Grid.Row="0" Style="{StaticResource PanelStyle}">
                    <DockPanel>
                        <TextBlock Text="Quest Workspace" Style="{StaticResource PanelHeaderStyle}" DockPanel.Dock="Top"/>
                        <views:WorkspaceControl DataContext="{Binding}"/>
                    </DockPanel>
                </Border>

                <!-- Code View Splitter -->
                <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Center" Style="{StaticResource SplitterStyle}" Visibility="{Binding IsCodeVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- Generated Code Panel -->
                <Border Grid.Row="2" Style="{StaticResource PanelStyle}" Visibility="{Binding IsCodeVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <DockPanel>
                        <DockPanel DockPanel.Dock="Top" Margin="8,8,8,4">
                            <TextBlock Text="Generated C# Code" Style="{StaticResource PanelHeaderStyle}" DockPanel.Dock="Left"/>
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                                <Button Content="Export"
                                        Style="{StaticResource IconButtonStyle}"
                                        Margin="4,0,0,0"
                                        Command="{Binding ExportCodeCommand}"/>
                                <Button Content="Copy"
                                        Style="{StaticResource IconButtonStyle}"
                                        Margin="4,0,0,0"
                                        Command="{Binding CopyCodeCommand}"/>
                            </StackPanel>
                        </DockPanel>    
                        <avalonEdit:TextEditor
                            x:Name="CodeEditor"
                            FontFamily="Consolas"
                            FontSize="12"
                            ShowLineNumbers="True"
                            utils:TextEditorHelper.BindableText="{Binding GeneratedCode}"
                            IsReadOnly="True"
                            Margin="8,0,8,8"/>
                    </DockPanel>
                </Border>
            </Grid>

            <!-- Vertical Splitter -->
            <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Style="{StaticResource SplitterStyle}"/>

            <!-- Properties Panel -->
            <Border Grid.Column="4" Style="{StaticResource PanelStyle}">
                <DockPanel>
                    <TextBlock Text="Quest Properties" Style="{StaticResource PanelHeaderStyle}" DockPanel.Dock="Top"/>
                    <views:PropertiesControl DataContext="{Binding}"/>
                </DockPanel>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StatusBarItem>
                <TextBlock Text="Ready"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Quests: "/>
                    <TextBlock Text="{Binding CurrentProject.Quests.Count}"/>
                    <TextBlock Text=" | Selected: " Margin="8,0,0,0"/>
                    <TextBlock Text="{Binding SelectedQuest.DisplayName, FallbackValue='None'}"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
