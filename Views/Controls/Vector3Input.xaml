<UserControl x:Class="Schedule1ModdingTool.Views.Controls.Vector3Input"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,4,0">
            <Label Content="X" Style="{StaticResource LabelStyle}" FontSize="11"/>
            <TextBox x:Name="XTextBox"
                     Text="{Binding X, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=LostFocus, StringFormat=F3}"
                     Style="{StaticResource TextBoxStyle}"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="4,0,4,0">
            <Label Content="Y" Style="{StaticResource LabelStyle}" FontSize="11"/>
            <TextBox x:Name="YTextBox"
                     Text="{Binding Y, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=LostFocus, StringFormat=F3}"
                     Style="{StaticResource TextBoxStyle}"/>
        </StackPanel>

        <StackPanel Grid.Column="2" Margin="4,0,0,0">
            <Label Content="Z" Style="{StaticResource LabelStyle}" FontSize="11"/>
            <TextBox x:Name="ZTextBox"
                     Text="{Binding Z, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=LostFocus, StringFormat=F3}"
                     Style="{StaticResource TextBoxStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>
