{
  "name": "schedule-one-model-preview",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build ./viewer.js --outdir ./dist --minify --target browser"
  },
  "dependencies": {
    "three": "0.186.1"
  }
}
