<Window x:Class="Schedule1ModdingTool.Views.CrashRecoveryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Crash Recovery - Schedule 1 Modding Tool"
        Height="500"
        Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource PrimaryHueMidBrush}"
                Padding="24,16">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="AlertCircle"
                                           Width="32"
                                           Height="32"
                                           Foreground="White"
                                           VerticalAlignment="Center"
                                           Margin="0,0,12,0"/>
                    <TextBlock Text="Unsaved Changes Detected"
                             Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                             Foreground="White"
                             VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Text="The application closed unexpectedly. Would you like to recover your unsaved work?"
                         Style="{StaticResource MaterialDesignBody1TextBlock}"
                         Foreground="White"
                         Opacity="0.87"
                         Margin="0,8,0,0"
                         TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1"
                     VerticalScrollBarVisibility="Auto"
                     Margin="24,16">
            <StackPanel>
                <TextBlock Text="Recoverable Projects"
                         Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                         Margin="0,0,0,12"/>

                <ItemsControl x:Name="RecoverableProjectsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{DynamicResource MaterialDesignDivider}"
                                  BorderThickness="1"
                                  CornerRadius="4"
                                  Padding="16"
                                  Margin="0,0,0,12"
                                  Background="{DynamicResource MaterialDesignCardBackground}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Project Name -->
                                    <TextBlock Grid.Row="0"
                                             Text="{Binding ProjectName}"
                                             Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                             FontWeight="Medium"/>

                                    <!-- Timestamp and Size -->
                                    <StackPanel Grid.Row="1"
                                              Orientation="Horizontal"
                                              Margin="0,4,0,0">
                                        <materialDesign:PackIcon Kind="ClockOutline"
                                                               Width="16"
                                                               Height="16"
                                                               VerticalAlignment="Center"
                                                               Margin="0,0,4,0"/>
                                        <TextBlock Text="{Binding Timestamp, StringFormat='Last saved: {0:g}'}"
                                                 Style="{StaticResource MaterialDesignBody2TextBlock}"
                                                 Opacity="0.68"
                                                 VerticalAlignment="Center"
                                                 Margin="0,0,16,0"/>

                                        <materialDesign:PackIcon Kind="File"
                                                               Width="16"
                                                               Height="16"
                                                               VerticalAlignment="Center"
                                                               Margin="0,0,4,0"/>
                                        <TextBlock Text="{Binding SizeDisplay}"
                                                 Style="{StaticResource MaterialDesignBody2TextBlock}"
                                                 Opacity="0.68"
                                                 VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <!-- Action Buttons -->
                                    <StackPanel Grid.Row="2"
                                              Orientation="Horizontal"
                                              Margin="0,12,0,0">
                                        <Button Content="RECOVER"
                                              Style="{StaticResource MaterialDesignFlatButton}"
                                              Command="{Binding DataContext.RecoverCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                              CommandParameter="{Binding}"
                                              Margin="0,0,8,0"/>
                                        <Button Content="DISCARD"
                                              Style="{StaticResource MaterialDesignFlatButton}"
                                              Command="{Binding DataContext.DiscardCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                              CommandParameter="{Binding}"
                                              Foreground="{DynamicResource MaterialDesignErrorBrush}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- No Projects Message -->
                <Border x:Name="NoProjectsMessage"
                      Visibility="Collapsed"
                      BorderBrush="{DynamicResource MaterialDesignDivider}"
                      BorderThickness="1"
                      CornerRadius="4"
                      Padding="24"
                      Background="{DynamicResource MaterialDesignCardBackground}">
                    <StackPanel HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="CheckCircle"
                                               Width="48"
                                               Height="48"
                                               HorizontalAlignment="Center"
                                               Foreground="{DynamicResource SuccessBrush}"
                                               Margin="0,0,0,12"/>
                        <TextBlock Text="No recoverable projects found"
                                 Style="{StaticResource MaterialDesignBody1TextBlock}"
                                 HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="0,1,0,0"
                Padding="24,16">
            <StackPanel Orientation="Horizontal"
                       HorizontalAlignment="Right">
                <Button Content="SKIP RECOVERY"
                      Style="{StaticResource MaterialDesignFlatButton}"
                      Click="SkipRecovery_Click"
                      Margin="0,0,8,0"/>
                <Button Content="CLOSE"
                      Style="{StaticResource MaterialDesignRaisedButton}"
                      Click="Close_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
